#!/bin/sh

set -eu

remote_name="${1:-origin}"
remote_url="${2:-}"
hook_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec "$hook_dir/verify-github-identity.sh" "推送前检查" "$remote_name" "$remote_url"
